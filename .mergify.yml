merge_queue:
  mode: isolated
  queue_controls_comment: false

queue_rules:
  - name: vm4-isolated-samehead-review-240924
    queue_conditions:
      - "#approved-reviews-by >= 1"
    merge_conditions:
      - check-success = vm4-isolated-hold-240924
    merge_method: merge
    batch_size: 1
    batch_max_wait_time: "1 second"
    checks_timeout: null
