scopes:
  source:
    files:
      docs:
        include:
          - "docs/**"
      security:
        include:
          - "security/**"

merge_queue:
  queue_controls_comment: false

queue_rules:
  - name: vm4 stack review gate 240924
    queue_conditions:
      - "#approved-reviews-by >= 1"
    merge_conditions:
      - check-success = vm4-hold-check-240924
    merge_method: merge
    batch_size: 2
    batch_max_wait_time: "5 minutes"
    checks_timeout: null
